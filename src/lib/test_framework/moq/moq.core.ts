// import { Mole } from '../setup/extensions';

export class Moq {
    // static create<T>(type: Function & { prototype: T }): Mole<T> {
    //     return new Mole<T>(Object.create(type.prototype));
    // }
}